{
   "name": "sistema-gestao-restaurante",
   "version": "1.0.0",
   "description": "Sistema de gestão de restaurante com múltiplos perfis de usuário",
   "main": "index.js",
   "scripts": {
      "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
      "server:dev": "cd server && npm run dev",
      "client:dev": "cd client && npm start",
      "build": "cd server && npm run build",
      "start": "cd server && npm start",
      "server:build": "cd server && npm run build",
      "client:build": "cd client && npm run build",
      "install:all": "npm install && cd server && npm install && cd ../client && npm install",
      "setup": "npm run install:all && cd server && npx prisma generate && npx prisma db push",
      "postinstall": "cd server && npm install && npx prisma generate"
   },
   "keywords": [
      "restaurante",
      "gestão",
      "nodejs",
      "react",
      "typescript"
   ],
   "author": "Victor Rony",
   "license": "MIT",
   "devDependencies": {
      "concurrently": "^7.6.0"
   }
}
